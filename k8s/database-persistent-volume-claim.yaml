apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: database-persistent-volume-Claim
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        request:
            storage: 2Gi
        